// Copy this file to src/test/data_struct_algo/<topic>/<problem_name>/Tests.cpp alongside the Code.cpp template from src/main/resources/template/.
// The include path below has one fewer "../" once copied there — see the sibling Tests.cpp files under src/test/data_struct_algo/ for the working pattern.
#include "../../../../../main/data_struct_algo/<topic>/<problem_name>/Code.cpp"
#include <cassert>
#include <iostream>

int main() {
    // Code obj;
    // assert(obj.methodName(input) == expected);

    std::cout << "All tests passed." << std::endl;
    return 0;
}
